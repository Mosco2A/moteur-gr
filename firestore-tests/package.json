{
  "name": "stepways-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Tests des regles de securite Firestore (P0-2 audit #327) — a executer via l emulateur, voir README",
  "scripts": {
    "test": "node --test rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0"
  }
}
