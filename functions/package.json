{
  "name": "stepways-functions",
  "private": true,
  "description": "Cloud Functions StepWays — calcul serveur des classements (Phase 7). Agregation par tranche avec k-anonymat (k>=5), libelles pseudonymes, sans timestamp fin (R1/R2 design D2 #86160).",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
